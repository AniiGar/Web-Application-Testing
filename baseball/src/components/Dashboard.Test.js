import Dashboard from './Dashboard';

describe('Testing hitHandler to increment by 1', () => {
    it('hits + 1', () => {
        const expected = 1;
        const actual = help.sum(0, 1);
      });
  }); 